// Per-origin grants were removed in browser protocol v3.
// Orbit now relies on required HTTP/HTTPS host permissions and Chrome's single
// global Site access setting while retaining controlled-tab and protected-target checks.
