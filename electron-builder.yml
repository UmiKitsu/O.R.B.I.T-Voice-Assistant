appId: com.umikitsune.orbit
productName: Orbit Voice Assistant
directories:
  buildResources: build
files:
  - '!**/.vscode/*'
  - '!src/*'
  - '!test/**'
  - '!scripts/smoke-wake-word.mjs'
  - '!resources/whisper/**'
  - '!resources/wake-word/**'
  - '!resources/kokoro/**'
  - '!resources/node-runtime/**'
  - '!electron.vite.config.{js,ts,mjs,cjs}'
  - '!{.eslintcache,eslint.config.mjs,.prettierignore,.prettierrc.yaml,dev-app-update.yml,CHANGELOG.md,README.md}'
  - '!{.env,.env.*,.npmrc,pnpm-lock.yaml}'
  - '!{tsconfig.json,tsconfig.node.json,tsconfig.web.json}'
asarUnpack:
  - resources/**
  - node_modules/loudness/impl/windows/*.exe
  - node_modules/sherpa-onnx-node/**
  - node_modules/sherpa-onnx-win-x64/**
win:
  executableName: orbit-voice-assistant
  target:
    - nsis
    - portable
extraResources:
  - from: resources/whisper
    to: whisper
    filter:
      - '**/*'
  - from: resources/kokoro
    to: kokoro
    filter:
      - '**/*'
  - from: resources/node-runtime
    to: node-runtime
    filter:
      - 'node.exe'
  - from: resources/wake-word
    to: wake-word
    filter:
      - '**/*'
  - from: resources/orbit-browser-extension
    to: orbit-browser-extension
    filter:
      - '**/*'
  - from: resources/windows-fixed-operations.ps1
    to: windows-fixed-operations.ps1
nsis:
  artifactName: ${name}-${version}-setup.${ext}
  oneClick: false
  allowToChangeInstallationDirectory: true
  shortcutName: ${productName}
  uninstallDisplayName: ${productName}
  createDesktopShortcut: always
mac:
  entitlementsInherit: build/entitlements.mac.plist
  extendInfo:
    - NSCameraUsageDescription: Application requests access to the device's camera.
    - NSMicrophoneUsageDescription: Application requests access to the device's microphone.
    - NSDocumentsFolderUsageDescription: Application requests access to the user's Documents folder.
    - NSDownloadsFolderUsageDescription: Application requests access to the user's Downloads folder.
  notarize: false
dmg:
  artifactName: ${name}-${version}.${ext}
linux:
  target:
    - AppImage
    - snap
    - deb
  maintainer: electronjs.org
  category: Utility
appImage:
  artifactName: ${name}-${version}.${ext}
npmRebuild: false
publish:
  provider: generic
  url: https://example.com/auto-updates
